# Output folder

New prompt files (`*.prompt.txt`) and generated animations (`*.svg`) land
here. Double-click any `.svg` to watch it in your browser.

When an animation is good enough for the real pet, an adult moves it into the
app following `guides/mode3-ship-it.pdf`.
